def solution(n):
    c1 = 0
    c2 = 1
    for _ in range(n):
        c3 = (c1 + c2)%1234567
        c1 = c2
        c2 = c3
    return c3

"""
임의의 숫자 n에서 k개수 만큼의 방법이 있다고 하면 n+1에는 k만큼의 방법의 뒤에 1칸이 붙은 방법이 생성되어 k만큼이 추가되고
n+2에서도 k만큼의 방법의 뒤에 2칸이 붙은 방법이 생성되어 k만큼이 추가된다.

이는 피보나치 수열과 동일한 방식으로 수가 증가하게 된다.
따라서 피보나치 수열을 구현하여 풀었다.
"""